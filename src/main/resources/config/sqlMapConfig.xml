<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
 PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

<!-- 	<properties resource="config/db.properties"></properties> -->
	<settings>
		<setting name="lazyLoadingEnabled" value="true" />
		<setting name="aggressiveLazyLoading" value="false" />
	</settings>

	<typeAliases>
		<!-- 批量定义别名 -->
		<package name="com.zwb.entity" />

		<!-- <typeAlias type="com.lucia.entity.User" alias="user"/> -->
		<!-- <typeAlias type="com.lucia.entity.Orders" alias="orders"/> -->
	</typeAliases>
	<!-- <plugins>
		<plugin interceptor="com.lucia.util.PageHandler">
			<property name="dialect" value="mysql" />
			<property name="pageSqlId" value=".*findUserById.*" />
		</plugin>
		<plugin interceptor="com.github.pagehelper.PageInterceptor">
			<property name="helperDialect" value="mysql" />
			<property name="reasonable" value="true" />
		</plugin>
	</plugins> -->
	<!-- <environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments> -->
	<!-- <mappers>
		 <mapper resource="dbResource/UserMapper.xml" /> 
		<package name="dbResource" /> 
		
	</mappers> -->
</configuration>
